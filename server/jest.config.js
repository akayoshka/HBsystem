module.exports = {
    // Тестовая среда, которая будет использоваться
    testEnvironment: "node",
    
    // Шаблоны для определения тестовых файлов
    testMatch: ["**/__tests__/**/*.test.js"],
    
    // Массив регулярных выражений, которые соответствуют игнорируемым путям тестов
    testPathIgnorePatterns: ["/node_modules/"],
    
    // Карта регулярных выражений и путей к трансформерам
    transform: {},
    
    // Увеличенный таймаут для тестов
    testTimeout: 30000,
    
    // Массив регулярных выражений, соответствующих всем путям к исходным файлам
    // перед повторным запуском тестов, связанных с измененными файлами
    watchPathIgnorePatterns: ["node_modules"],
    
    // Указывает, должен ли каждый отдельный тест сообщаться во время выполнения
    verbose: true,
    
    // Автоматически очищать моки вызовов и экземпляров между каждым тестом
    clearMocks: true,
    
    // Директория, куда Jest должен выводить файлы покрытия
    coverageDirectory: "coverage",
    
    // Указывает, какой поставщик должен использоваться для инструментирования кода для покрытия
    coverageProvider: "v8",
    
    // Список путей к директориям, которые Jest должен использовать для поиска файлов
    roots: ["<rootDir>"],
    
    // Путь к модулям, которые запускают код для настройки тестовой среды
    setupFiles: ["<rootDir>/__tests__/setup.js"],
    
    // Завершить тестовый запуск после определенного времени (в миллисекундах)
    // Это может помочь с зависшими тестами
    testTimeout: 30000,
    
    // Принудительно завершить процесс Node.js после запуска всех тестов
    // Это помогает с проблемами открытых обработчиков
    forceExit: true,
    
    // Колличество одновременно выполняемых тестов
    // Уменьшите это значение, если у вас возникают проблемы с памятью
    maxWorkers: "50%",
    
    // Глобальные переменные, доступные во всех тестовых файлах
    globals: {
      // Добавьте здесь глобальные переменные, если они нужны
    }
  };